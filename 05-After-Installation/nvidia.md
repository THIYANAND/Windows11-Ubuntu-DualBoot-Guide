# 🎮 Installing NVIDIA GPU Drivers

To get the best performance and prevent screen tearing or battery drain, you must install the official NVIDIA drivers.

1. Open a terminal.
2. Auto-install the recommended proprietary driver:
   ```bash
   sudo ubuntu-drivers autoinstall

Reboot your PC.

Verify the installation by opening a terminal and typing:

    ```bash
   nvidia-smi